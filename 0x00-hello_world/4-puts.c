#include <stdio.h>
int main(void)
{
char *message = "\"Programming is like building a multilingual puzzle";
/**
 * Using puts to print the string
 * main - Entry point
*/
puts(message);
return (0);
}
