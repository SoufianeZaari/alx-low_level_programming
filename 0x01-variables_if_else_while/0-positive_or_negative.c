#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int n;
	if (n>0)
		printf("%d is positive",&d);
	if else (n=0)
		printf("%d is zero",&d);
	else
		printf("%d is negative",&d);
	return (0);
}
